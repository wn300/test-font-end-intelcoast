import { Image } from '../../entity/image';

/**
 * estado inicial del objeto de imagenes
 */
export interface ImageState {
  /**
   * objeto de la imagen cuando se peticiona
   */
  imageObject: Image;
}

